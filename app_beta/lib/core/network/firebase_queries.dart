

class FirebaseApi {
  // static Stream<List<ChatUser>> getUsers() => FirebaseFirestore.instance
  //     .collection('users')
  //     .orderBy(ChatUserField.lastMessageTime, descending: true)
  //     .snapshots()
  //     .transform(Utils.transformer(ChatUser.fromJson));
}
